cassandra-remote-control
========================

A python daemon to manage cassandra cluster recurrent jobs.

~ Configuration ~

Edit the file `config.ini`, set the proper configuration to connect to cassandra
cluster in the section `[cassandra]`.

= How to add new command =

Add a subsection under `[command]`
